<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#121212">

    <EditText android:id="@+id/numara"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Hedef Numara"
        android:textColorHint="#999"
        android:textColor="#FFF" />

    <EditText android:id="@+id/mesaj"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Mesaj Metni"
        android:textColorHint="#999"
        android:textColor="#FFF" />

    <Button android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SMS GÖNDER"
        android:onClick="smsGonder"
        android:backgroundTint="#008069" />

    <Button android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ARAMA YAP"
        android:onClick="aramaYap"
        android:backgroundTint="#d32f2f" />
</LinearLayout>
